description "elife-dashboard-process-queue-daemon - pass an ID to distinguish between them"
respawn
setuid {{ pillar.elife.deploy_user.username }}
instance $ID
chdir /srv/elife-dashboard/
exec ./process_dashboard_queue.sh
